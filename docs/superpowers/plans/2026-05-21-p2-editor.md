# 笔记应用编辑器集成 实施计划 (P2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 集成 TipTap 编辑器，实现文本/Markdown 笔记的创建、编辑和持久化，支持 WYSIWYG ↔ MD 源码模式切换。

**Architecture:** TipTap (ProseMirror) 作为编辑器核心。桌面端默认 WYSIWYG 模式，移动端默认 Markdown 源码模式。编辑内容变更通过 debounce 500ms 自动保存到 SQLite（通过 StorageAdapter）。ProseMirror JSON 和 Markdown 双向同步存储。

**Tech Stack:** TipTap v2, @tiptap/react, @tiptap/starter-kit, @tiptap/pm, tiptap-markdown (序列化), React 18, Zustand

**设计规格:** `docs/superpowers/specs/2026-05-21-notes-app-design.md`

**前置条件:** P0+P1 已完成（StorageAdapter、数据模型、Zustand Store 均可用）

---

## 文件结构

```
packages/web/src/
├── components/
│   ├── shared/
│   │   ├── Editor.tsx              # TipTap 编辑器主组件
│   │   ├── EditorToolbar.tsx       # 编辑器工具栏（桌面端）
│   │   ├── MarkdownEditor.tsx      # Markdown 源码编辑器
│   │   ├── NoteCard.tsx            # 笔记列表卡片
│   │   └── TagBadge.tsx            # 标签徽章
│   ├── desktop/
│   │   └── Sidebar.tsx             # 桌面端侧栏（笔记列表）
│   ├── mobile/
│   │   └── MobileDrawer.tsx        # 移动端抽屉
│   │   └── NoteListMobile.tsx      # 移动端笔记列表
│   ├── layouts/
│   │   ├── DesktopLayout.tsx       # 桌面端双栏布局
│   │   └── MobileLayout.tsx        # 移动端堆栈布局
│   ├── QuickNote.tsx               # 首屏快速笔记组件
│   └── NoteView.tsx                # 笔记详情/编辑视图
├── hooks/
│   ├── useResponsive.ts            # 响应式布局检测
│   ├── useAutoSave.ts              # (已实现，需增强)
│   └── useStorage.ts               # StorageAdapter 操作 hook
│   └── index.ts
├── stores/
│   ├── notesStore.ts               # (已实现，需增强)
│   └── index.ts                    # (已实现)
├── lib/
│   ├── tiptap-setup.ts             # TipTap 扩展配置
│   ├── markdown-serializer.ts      # MD ↔ ProseMirror 转换
│   └── index.ts                    # (已实现)
└── App.tsx                         # 应用根组件（更新为包含布局和路由）

packages/web/tests/
├── components/
│   ├── Editor.test.tsx             # 编辑器组件测试
│   ├── QuickNote.test.tsx          # 快速笔记测试
│   └── NoteView.test.tsx           # 笔记视图测试
│   └── MarkdownEditor.test.tsx     # MD 编辑器测试
└── hooks/
│   ├── useResponsive.test.ts       # 响应式 hook 测试
│   └── useStorage.test.ts          # 存储 hook 测试
```

---

### Task 12: 安装 TipTap 依赖并配置

**Files:**
- Modify: `packages/web/package.json` (添加 TipTap 相关依赖)
- Create: `packages/web/src/lib/tiptap-setup.ts`

- [ ] **Step 1: 添加 TipTap 依赖到 packages/web/package.json**

在 dependencies 中添加：
```
"@tiptap/react": "^2",
"@tiptap/starter-kit": "^2",
"@tiptap/pm": "^2",
"@tiptap/extension-placeholder": "^2",
"@tiptap/extension-character-count": "^2"
```

Run: `pnpm install`

- [ ] **Step 2: 编写 tiptap-setup.ts**

```typescript
// packages/web/src/lib/tiptap-setup.ts
import StarterKit from "@tiptap/starter-kit";
import Placeholder from "@tiptap/extension-placeholder";
import CharacterCount from "@tiptap/extension-character-count";

export function getEditorExtensions(isMobile: boolean) {
  const extensions = [
    StarterKit.configure({
      heading: { levels: [1, 2, 3] },
      bulletList: { keepMarks: true },
      orderedList: { keepMarks: true },
    }),
    Placeholder.configure({
      placeholder: isMobile ? "开始记录..." : "想写点什么？",
    }),
    CharacterCount,
  ];
  return extensions;
}
```

- [ ] **Step 3: Commit**

```bash
git add packages/web/package.json packages/web/src/lib/tiptap-setup.ts pnpm-lock.yaml
git commit -m "feat: 安装 TipTap 依赖并配置基础扩展"
```

---

### Task 13: Markdown 序列化器

**Files:**
- Create: `packages/web/src/lib/markdown-serializer.ts`
- Test: `packages/web/tests/lib/markdown-serializer.test.ts`

TipTap 没有官方的 markdown 扩展包可直接使用。我们需要自己实现 ProseMirror JSON ↔ Markdown 的双向转换。使用 `prosemirror-markdown` 库作为底层。

- [ ] **Step 1: 添加 prosemirror-markdown 依赖**

在 `packages/web/package.json` 的 dependencies 中添加：
```
"prosemirror-markdown": "^1"
```

Run: `pnpm install`

- [ ] **Step 2: 编写 markdown-serializer.ts**

```typescript
// packages/web/src/lib/markdown-serializer.ts
import { defaultMarkdownParser, defaultMarkdownSerializer } from "prosemirror-markdown";
import { Schema, DOMParser, DOMSerializer } from "prosemirror-model";
import { schema as basicSchema } from "@tiptap/pm";

export function markdownToProseMirrorJSON(md: string): string {
  if (!md.trim()) return '{"type":"doc","content":[]}';
  const doc = defaultMarkdownParser.parse(md);
  const json = doc.toJSON();
  return JSON.stringify(json);
}

export function proseMirrorJSONToMarkdown(jsonStr: string): string {
  if (!jsonStr.trim()) return "";
  try {
    const parsed = JSON.parse(jsonStr);
    const doc = basicSchema.nodeFromJSON(parsed);
    return defaultMarkdownSerializer.serialize(doc);
  } catch {
    return "";
  }
}

export function extractTitleFromContent(content: string): string {
  const lines = content.split("\n").filter((l) => l.trim());
  if (lines.length === 0) return "未命名笔记";
  const firstLine = lines[0].replace(/^#+\s*/, "").trim();
  return firstLine.slice(0, 50) || "未命名笔记";
}
```

- [ ] **Step 3: 编写序列化器测试**

```typescript
// packages/web/tests/lib/markdown-serializer.test.ts
import { describe, it, expect } from "vitest";
import {
  markdownToProseMirrorJSON,
  proseMirrorJSONToMarkdown,
  extractTitleFromContent,
} from "../../src/lib/markdown-serializer";

describe("Markdown 序列化器", () => {
  it("markdownToProseMirrorJSON 应将 MD 转为 ProseMirror JSON", () => {
    const md = "# 标题\n\n段落内容";
    const json = markdownToProseMirrorJSON(md);
    const parsed = JSON.parse(json);
    expect(parsed.type).toBe("doc");
    expect(parsed.content.length).toBeGreaterThan(0);
  });

  it("markdownToProseMirrorJSON 空字符串应返回空文档", () => {
    const json = markdownToProseMirrorJSON("");
    const parsed = JSON.parse(json);
    expect(parsed.type).toBe("doc");
    expect(parsed.content).toEqual([]);
  });

  it("proseMirrorJSONToMarkdown 应将 ProseMirror JSON 转回 MD", () => {
    const md = "# 标题\n\n段落内容";
    const json = markdownToProseMirrorJSON(md);
    const resultMd = proseMirrorJSONToMarkdown(json);
    expect(resultMd).toContain("标题");
    expect(resultMd).toContain("段落内容");
  });

  it("proseMirrorJSONToMarkdown 空字符串应返回空字符串", () => {
    const result = proseMirrorJSONToMarkdown("");
    expect(result).toBe("");
  });

  it("extractTitleFromContent 应从内容提取标题", () => {
    expect(extractTitleFromContent("# 我的笔记\n\n内容")).toBe("我的笔记");
    expect(extractTitleFromContent("没有标题标记的内容")).toBe("没有标题标记的内容");
    expect(extractTitleFromContent("")).toBe("未命名笔记");
  });

  it("extractTitleFromContent 应截断超过50字符的标题", () => {
    const longTitle = "这是一个非常非常非常非常非常非常非常非常非常非常非常长的标题内容";
    expect(extractTitleFromContent(longTitle).length).toBeLessThanOrEqual(50);
  });
});
```

- [ ] **Step 4: 运行测试**

Run: `cd packages/web && pnpm vitest run`

注意：需要先在 packages/web/vitest.config.ts 中配置测试环境。

Expected: 序列化器测试通过

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/lib/markdown-serializer.ts packages/web/tests/ packages/web/package.json pnpm-lock.yaml
git commit -m "feat: 实现 Markdown ↔ ProseMirror JSON 双向序列化器"
```

---

### Task 14: 响应式布局 Hook

**Files:**
- Create: `packages/web/src/hooks/useResponsive.ts`
- Test: `packages/web/tests/hooks/useResponsive.test.ts`

- [ ] **Step 1: 编写 useResponsive hook**

```typescript
// packages/web/src/hooks/useResponsive.ts
import { useState, useEffect } from "react";
import { useUIStore } from "../stores";

export type DeviceType = "mobile" | "tablet" | "desktop";

export interface ResponsiveState {
  device: DeviceType;
  isMobile: boolean;
  isTablet: boolean;
  isDesktop: boolean;
  width: number;
}

export function useResponsive(): ResponsiveState {
  const [width, setWidth] = useState(window.innerWidth);
  const setIsMobile = useUIStore((s) => s.setIsMobile);

  useEffect(() => {
    const handleResize = () => {
      const newWidth = window.innerWidth;
      setWidth(newWidth);
      setIsMobile(newWidth < 768);
    };
    window.addEventListener("resize", handleResize);
    handleResize();
    return () => window.removeEventListener("resize", handleResize);
  }, [setIsMobile]);

  const device: DeviceType = width < 640 ? "mobile" : width < 768 ? "tablet" : "desktop";

  return {
    device,
    isMobile: width < 640,
    isTablet: width >= 640 && width < 768,
    isDesktop: width >= 768,
    width,
  };
}
```

- [ ] **Step 2: 编写 useResponsive 测试**

```typescript
// packages/web/tests/hooks/useResponsive.test.ts
import { describe, it, expect } from "vitest";

describe("useResponsive 工具", () => {
  it("DeviceType 应包含 mobile、tablet、desktop", () => {
    const types = ["mobile", "tablet", "desktop"];
    expect(types).toHaveLength(3);
  });

  it("640px 以下应判定为 mobile", () => {
    const width = 320;
    const device = width < 640 ? "mobile" : width < 768 ? "tablet" : "desktop";
    expect(device).toBe("mobile");
    expect(width < 640).toBe(true);
    expect(width < 768).toBe(true);
  });

  it("640-768px 应判定为 tablet", () => {
    const width = 700;
    const device = width < 640 ? "mobile" : width < 768 ? "tablet" : "desktop";
    expect(device).toBe("tablet");
  });

  it("768px 以上应判定为 desktop", () => {
    const width = 1024;
    const device = width < 640 ? "mobile" : width < 768 ? "tablet" : "desktop";
    expect(device).toBe("desktop");
  });
});
```

- [ ] **Step 3: 更新 hooks/index.ts**

```typescript
// packages/web/src/hooks/index.ts
export { useAutoSave } from "./useAutoSave";
export { useResponsive, DeviceType, ResponsiveState } from "./useResponsive";
```

- [ ] **Step 4: Commit**

```bash
git add packages/web/src/hooks/useResponsive.ts packages/web/tests/hooks/ packages/web/src/hooks/index.ts
git commit -m "feat: 实现 useResponsive 响应式布局检测 hook"
```

---

### Task 15: StorageAdapter 操作 Hook

**Files:**
- Create: `packages/web/src/hooks/useStorage.ts`

- [ ] **Step 1: 编写 useStorage hook**

```typescript
// packages/web/src/hooks/useStorage.ts
import { useCallback } from "react";
import { getStorage } from "../lib";
import {
  Note, CreateNoteInput, UpdateNoteInput,
  Folder, CreateFolderInput,
  Tag,
  SearchInput, SearchResult,
} from "@notes/core";

export function useStorage() {
  const createNote = useCallback(async (input: CreateNoteInput): Promise<Note> => {
    const storage = getStorage();
    return storage.createNote(input);
  }, []);

  const updateNote = useCallback(async (id: string, input: UpdateNoteInput): Promise<Note> => {
    const storage = getStorage();
    return storage.updateNote(id, input);
  }, []);

  const deleteNote = useCallback(async (id: string): Promise<void> => {
    const storage = getStorage();
    return storage.deleteNote(id);
  }, []);

  const getNote = useCallback(async (id: string): Promise<Note | null> => {
    const storage = getStorage();
    return storage.getNote(id);
  }, []);

  const listNotes = useCallback(async (folderId?: string, tagId?: string): Promise<Note[]> => {
    const storage = getStorage();
    return storage.listNotes(folderId, tagId);
  }, []);

  const createFolder = useCallback(async (input: CreateFolderInput): Promise<Folder> => {
    const storage = getStorage();
    return storage.createFolder(input);
  }, []);

  const listFolders = useCallback(async (parentId?: string | null): Promise<Folder[]> => {
    const storage = getStorage();
    return storage.listFolders(parentId);
  }, []);

  const createTag = useCallback(async (name: string): Promise<Tag> => {
    const storage = getStorage();
    return storage.createTag(name);
  }, []);

  const addTagsToNote = useCallback(async (noteId: string, tagIds: string[]): Promise<void> => {
    const storage = getStorage();
    return storage.addTagsToNote(noteId, tagIds);
  }, []);

  const listTags = useCallback(async (): Promise<Tag[]> => {
    const storage = getStorage();
    return storage.listTags();
  }, []);

  const searchNotes = useCallback(async (input: SearchInput): Promise<SearchResult> => {
    const storage = getStorage();
    return storage.searchNotes(input);
  }, []);

  return {
    createNote, updateNote, deleteNote, getNote, listNotes,
    createFolder, listFolders,
    createTag, addTagsToNote, listTags,
    searchNotes,
  };
}
```

- [ ] **Step 2: 更新 hooks/index.ts**

```typescript
// packages/web/src/hooks/index.ts
export { useAutoSave } from "./useAutoSave";
export { useResponsive, DeviceType, ResponsiveState } from "./useResponsive";
export { useStorage } from "./useStorage";
```

- [ ] **Step 3: Commit**

```bash
git add packages/web/src/hooks/useStorage.ts packages/web/src/hooks/index.ts
git commit -m "feat: 实现 useStorage hook（StorageAdapter 操作封装）"
```

---

### Task 16: TipTap 编辑器组件

**Files:**
- Create: `packages/web/src/components/shared/Editor.tsx`
- Create: `packages/web/src/components/shared/EditorToolbar.tsx`

- [ ] **Step 1: 编写 Editor.tsx**

```typescript
// packages/web/src/components/shared/Editor.tsx
import { useEditor, EditorContent } from "@tiptap/react";
import { useEffect, useCallback } from "react";
import { getEditorExtensions } from "../lib/tiptap-setup";
import { useUIStore } from "../../stores";
import EditorToolbar from "./EditorToolbar";

interface EditorProps {
  content: string;
  onUpdate: (contentJson: string, mdText: string) => void;
  isMobile?: boolean;
}

export default function Editor({ content, onUpdate, isMobile }: EditorProps) {
  const editorMode = useUIStore((s) => s.editorMode);
  const isMobileDefault = useUIStore((s) => s.isMobile);
  const mobile = isMobile ?? isMobileDefault;

  const editor = useEditor({
    extensions: getEditorExtensions(mobile),
    content: content || "",
    onUpdate: ({ editor }) => {
      const contentJson = JSON.stringify(editor.getJSON());
      const mdText = editor.getText();
      onUpdate(contentJson, mdText);
    },
    editorProps: {
      attributes: {
        class: mobile
          ? "prose prose-sm max-w-none focus:outline-none min-h-[200px]"
          : "prose prose-lg max-w-none focus:outline-none min-h-[300px]",
      },
    },
  });

  useEffect(() => {
    if (editor && content) {
      const currentContent = JSON.stringify(editor.getJSON());
      if (currentContent !== content) {
        editor.commands.setContent(content);
      }
    }
  }, [content]);

  if (!editor) return null;

  return (
    <div className="editor-container">
      {!mobile && editorMode === "wysiwyg" && <EditorToolbar editor={editor} />}
      <EditorContent editor={editor} />
    </div>
  );
}
```

- [ ] **Step 2: 编写 EditorToolbar.tsx**

```typescript
// packages/web/src/components/shared/EditorToolbar.tsx
import { type Editor } from "@tiptap/react";

interface EditorToolbarProps {
  editor: Editor;
}

export default function EditorToolbar({ editor }: EditorToolbarProps) {
  if (!editor) return null;

  return (
    <div className="flex items-center gap-1 p-2 border-b border-gray-200 bg-gray-50 rounded-t-lg">
      <button
        onClick={() => editor.chain().focus().toggleBold().run()}
        className={`px-2 py-1 rounded text-sm font-bold ${
          editor.isActive("bold") ? "bg-blue-500 text-white" : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
        title="粗体"
      >
        B
      </button>
      <button
        onClick={() => editor.chain().focus().toggleItalic().run()}
        className={`px-2 py-1 rounded text-sm italic ${
          editor.isActive("italic") ? "bg-blue-500 text-white" : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
        title="斜体"
      >
        I
      </button>
      <button
        onClick={() => editor.chain().focus().toggleHeading({ level: 1 }).run()}
        className={`px-2 py-1 rounded text-sm ${
          editor.isActive("heading", { level: 1 }) ? "bg-blue-500 text-white" : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
        title="标题1"
      >
        H1
      </button>
      <button
        onClick={() => editor.chain().focus().toggleHeading({ level: 2 }).run()}
        className={`px-2 py-1 rounded text-sm ${
          editor.isActive("heading", { level: 2 }) ? "bg-blue-500 text-white" : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
        title="标题2"
      >
        H2
      </button>
      <button
        onClick={() => editor.chain().focus().toggleBulletList().run()}
        className={`px-2 py-1 rounded text-sm ${
          editor.isActive("bulletList") ? "bg-blue-500 text-white" : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
        title="无序列表"
      >
        • 列表
      </button>
      <button
        onClick={() => editor.chain().focus().toggleOrderedList().run()}
        className={`px-2 py-1 rounded text-sm ${
          editor.isActive("orderedList") ? "bg-blue-500 text-white" : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
        title="有序列表"
      >
        1. 列表
      </button>
      <button
        onClick={() => editor.chain().focus().toggleCodeBlock().run()}
        className={`px-2 py-1 rounded text-sm ${
          editor.isActive("codeBlock") ? "bg-blue-500 text-white" : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
        title="代码块"
      >
        { }代码
      </button>
      <div className="ml-auto flex items-center gap-2">
        <span className="text-xs text-gray-500">
          {editor.storage.characterCount?.characters() ?? 0} 字
        </span>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add packages/web/src/components/shared/Editor.tsx packages/web/src/components/shared/EditorToolbar.tsx
git commit -m "feat: 实现 TipTap 编辑器组件和工具栏"
```

---

### Task 17: Markdown 源码编辑器组件

**Files:**
- Create: `packages/web/src/components/shared/MarkdownEditor.tsx`

- [ ] **Step 1: 编写 MarkdownEditor.tsx**

```typescript
// packages/web/src/components/shared/MarkdownEditor.tsx
import { useState, useCallback, useEffect } from "react";

interface MarkdownEditorProps {
  content: string;
  onUpdate: (mdText: string) => void;
}

export default function MarkdownEditor({ content, onUpdate }: MarkdownEditorProps) {
  const [localContent, setLocalContent] = useState(content);
  const [timeoutId, setTimeoutId] = useState<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    setLocalContent(content);
  }, [content]);

  const handleChange = useCallback(
    (e: React.ChangeEvent<HTMLTextAreaElement>) => {
      const newContent = e.target.value;
      setLocalContent(newContent);

      if (timeoutId) clearTimeout(timeoutId);
      const newTimeout = setTimeout(() => {
        onUpdate(newContent);
      }, 500);
      setTimeoutId(newTimeout);
    },
    [onUpdate, timeoutId]
  );

  return (
    <textarea
      value={localContent}
      onChange={handleChange}
      className="w-full h-full p-4 font-mono text-sm bg-white border border-gray-200 rounded-lg resize-none focus:outline-none focus:border-blue-500"
      placeholder="开始编写 Markdown..."
      spellCheck={false}
    />
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/web/src/components/shared/MarkdownEditor.tsx
git commit -m "feat: 实现 Markdown 源码编辑器组件"
```

---

### Task 18: 模式切换（WYSIWYG ↔ MD）

**Files:**
- Create: `packages/web/src/components/shared/ModeToggle.tsx`

- [ ] **Step 1: 编写 ModeToggle.tsx**

```typescript
// packages/web/src/components/shared/ModeToggle.tsx
import { useUIStore } from "../../stores";

export default function ModeToggle() {
  const editorMode = useUIStore((s) => s.editorMode);
  const setEditorMode = useUIStore((s) => s.setEditorMode);

  return (
    <div className="flex items-center gap-1">
      <button
        onClick={() => setEditorMode("wysiwyg")}
        className={`px-3 py-1 rounded text-sm ${
          editorMode === "wysiwyg"
            ? "bg-blue-500 text-white"
            : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
      >
        所见即所得
      </button>
      <button
        onClick={() => setEditorMode("markdown")}
        className={`px-3 py-1 rounded text-sm ${
          editorMode === "markdown"
            ? "bg-blue-500 text-white"
            : "bg-gray-200 text-gray-700 hover:bg-gray-300"
        }`}
      >
        Markdown
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/web/src/components/shared/ModeToggle.tsx
git commit -m "feat: 实现编辑模式切换组件（WYSIWYG ↔ Markdown）"
```

---

### Task 19: 笔记视图组件（整合编辑器 + 模式切换 + 自动保存）

**Files:**
- Create: `packages/web/src/components/NoteView.tsx`

- [ ] **Step 1: 编写 NoteView.tsx**

```typescript
// packages/web/src/components/NoteView.tsx
import { useState, useCallback, useEffect } from "react";
import { useUIStore } from "../stores";
import { useStorage } from "../hooks";
import Editor from "./shared/Editor";
import MarkdownEditor from "./shared/MarkdownEditor";
import ModeToggle from "./shared/ModeToggle";
import EditorToolbar from "./shared/EditorToolbar";
import { markdownToProseMirrorJSON, proseMirrorJSONToMarkdown, extractTitleFromContent } from "../lib/markdown-serializer";
import { Note } from "@notes/core";

interface NoteViewProps {
  note: Note;
  onBack?: () => void;
}

export default function NoteView({ note, onBack }: NoteViewProps) {
  const editorMode = useUIStore((s) => s.editorMode);
  const isMobile = useUIStore((s) => s.isMobile);
  const { updateNote } = useStorage();
  const [contentJson, setContentJson] = useState(note.contentJson);
  const [mdText, setMdText] = useState(note.mdText);

  const handleWysiwygUpdate = useCallback(
    (newJson: string, newMd: string) => {
      setContentJson(newJson);
      setMdText(newMd);
    },
    []
  );

  const handleMarkdownUpdate = useCallback(
    (newMd: string) => {
      setMdText(newMd);
      const newJson = markdownToProseMirrorJSON(newMd);
      setContentJson(newJson);
    },
    []
  );

  useEffect(() => {
    const timeout = setTimeout(async () => {
      const title = extractTitleFromContent(mdText);
      await updateNote(note.id, {
        title: title !== note.title ? title : undefined,
        contentJson,
        mdText,
      });
    }, 500);
    return () => clearTimeout(timeout);
  }, [contentJson, mdText]);

  const displayContent = editorMode === "markdown" ? mdText : contentJson;

  return (
    <div className="flex flex-col h-full">
      <div className="flex items-center justify-between p-2 border-b border-gray-200">
        {isMobile && onBack && (
          <button onClick={onBack} className="px-2 py-1 text-sm text-blue-500 hover:text-blue-700">
            ← 返回
          </button>
        )}
        <h2 className="text-lg font-semibold text-gray-800">{note.title}</h2>
        <div className="flex items-center gap-2">
          <ModeToggle />
        </div>
      </div>

      <div className="flex-1 overflow-auto p-4">
        {editorMode === "wysiwyg" ? (
          <Editor content={displayContent} onUpdate={handleWysiwygUpdate} isMobile={isMobile} />
        ) : (
          <MarkdownEditor content={mdText} onUpdate={handleMarkdownUpdate} />
        )}
      </div>

      <div className="p-2 text-xs text-gray-500 border-t border-gray-200">
        {note.updatedAt.toLocaleDateString()} · 自动保存 ✓
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/web/src/components/NoteView.tsx
git commit -m "feat: 实现笔记视图组件（编辑器 + 模式切换 + 自动保存）"
```

---

### Task 20: 快速笔记组件（首屏）

**Files:**
- Create: `packages/web/src/components/QuickNote.tsx`
- Create: `packages/web/src/components/shared/NoteCard.tsx`
- Create: `packages/web/src/components/shared/TagBadge.tsx`

- [ ] **Step 1: 编写 NoteCard.tsx**

```typescript
// packages/web/src/components/shared/NoteCard.tsx
import { Note } from "@notes/core";
import TagBadge from "./TagBadge";

interface NoteCardProps {
  note: Note;
  onClick: (note: Note) => void;
  tags?: { id: string; name: string }[];
}

export default function NoteCard({ note, onClick, tags }: NoteCardProps) {
  const timeStr = new Date(note.updatedAt).toLocaleDateString("zh-CN", {
    month: "short",
    day: "numeric",
  });

  return (
    <button
      onClick={() => onClick(note)}
      className="block w-full p-3 bg-white border border-gray-200 rounded-lg hover:border-blue-300 hover:shadow-sm transition-colors text-left"
    >
      <div className="font-semibold text-gray-800 text-sm truncate">{note.title}</div>
      <div className="flex items-center gap-2 mt-1">
        <span className="text-xs text-gray-500">{timeStr}</span>
        {tags?.map((tag) => (
          <TagBadge key={tag.id} name={tag.name} />
        ))}
      </div>
    </button>
  );
}
```

- [ ] **Step 2: 编写 TagBadge.tsx**

```typescript
// packages/web/src/components/shared/TagBadge.tsx
interface TagBadgeProps {
  name: string;
  onClick?: () => void;
  removable?: boolean;
  onRemove?: () => void;
}

export default function TagBadge({ name, onClick, removable, onRemove }: TagBadgeProps) {
  return (
    <span
      onClick={onClick}
      className="inline-flex items-center gap-1 px-2 py-0.5 text-xs rounded-full bg-blue-100 text-blue-700 cursor-pointer hover:bg-blue-200"
    >
      {name}
      {removable && (
        <button
          onClick={(e) => { e.stopPropagation(); onRemove?.(); }}
          className="text-blue-500 hover:text-red-500 ml-0.5"
        >
          ×
        </button>
      )}
    </span>
  );
}
```

- [ ] **Step 3: 编写 QuickNote.tsx**

```typescript
// packages/web/src/components/QuickNote.tsx
import { useState, useCallback, useEffect } from "react";
import { useStorage } from "../hooks";
import { useNotesStore, useUIStore } from "../stores";
import { extractTitleFromContent } from "../lib/markdown-serializer";
import NoteCard from "./shared/NoteCard";

export default function QuickNote() {
  const { createNote, listNotes } = useStorage();
  const { notes, setNotes, addNote } = useNotesStore();
  const isMobile = useUIStore((s) => s.isMobile);
  const [inputValue, setInputValue] = useState("");
  const [currentQuickNoteId, setCurrentQuickNoteId] = useState<string | null>(null);

  useEffect(() => {
    listNotes().then(setNotes);
  }, []);

  const handleStartTyping = useCallback(async () => {
    if (currentQuickNoteId) return;
    const title = extractTitleFromContent(inputValue);
    const note = await createNote({ title, mdText: inputValue });
    addNote(note);
    setCurrentQuickNoteId(note.id);
  }, [inputValue, currentQuickNoteId]);

  const handleInputChange = useCallback(
    (e: React.ChangeEvent<HTMLTextAreaElement>) => {
      setInputValue(e.target.value);
      if (!currentQuickNoteId && e.target.value.trim()) {
        handleStartTyping();
      }
    },
    [currentQuickNoteId, handleStartTyping]
  );

  const handleNoteClick = useCallback((note: any) => {
    useNotesStore.getState().setCurrentNote(note);
  }, []);

  const recentNotes = notes.filter((n) => n.deletedAt === null).slice(0, 10);

  return (
    <div className="flex flex-col h-full p-4 max-w-2xl mx-auto">
      <div className="text-center mb-4">
        <h1 className="text-2xl font-bold text-gray-800">⚡ 快速笔记</h1>
        <p className="text-sm text-gray-500">开始输入 — 自动保存</p>
      </div>

      <textarea
        value={inputValue}
        onChange={handleInputChange}
        className={`w-full p-4 bg-gray-50 border-2 border-gray-200 rounded-xl resize-none focus:outline-none focus:border-blue-500 ${
          isMobile ? "min-h-[120px]" : "min-h-[160px]"
        }`}
        placeholder="想写点什么？"
      />

      <div className="mt-6">
        <h3 className="text-sm font-semibold text-gray-600 mb-2">最近笔记</h3>
        <div className="space-y-2">
          {recentNotes.map((note) => (
            <NoteCard key={note.id} note={note} onClick={handleNoteClick} />
          ))}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add packages/web/src/components/QuickNote.tsx packages/web/src/components/shared/NoteCard.tsx packages/web/src/components/shared/TagBadge.tsx
git commit -m "feat: 实现快速笔记首屏 + NoteCard + TagBadge 组件"
```

---

### Task 21: 桌面端布局 + 侧栏

**Files:**
- Create: `packages/web/src/components/layouts/DesktopLayout.tsx`
- Create: `packages/web/src/components/desktop/Sidebar.tsx`
- Modify: `packages/web/src/App.tsx`

- [ ] **Step 1: 编写 Sidebar.tsx**

```typescript
// packages/web/src/components/desktop/Sidebar.tsx
import { useEffect } from "react";
import { useStorage } from "../../hooks";
import { useNotesStore, useFoldersStore } from "../../stores";
import NoteCard from "../shared/NoteCard";

export default function Sidebar() {
  const { listNotes, listFolders } = useStorage();
  const { notes, setNotes, setCurrentNote } = useNotesStore();
  const { folders, setFolders } = useFoldersStore();

  useEffect(() => {
    listNotes().then(setNotes);
    listFolders().then(setFolders);
  }, []);

  const activeNotes = notes.filter((n) => n.deletedAt === null);

  return (
    <div className="w-[320px] h-full bg-gray-50 border-r border-gray-200 flex flex-col">
      <div className="p-3 border-b border-gray-200">
        <div className="flex items-center justify-between">
          <h2 className="text-sm font-semibold text-gray-700">全部笔记</h2>
          <span className="text-xs text-gray-500">▼ 文件夹</span>
        </div>
      </div>

      <div className="flex-1 overflow-auto p-2 space-y-1">
        {activeNotes.map((note) => (
          <NoteCard key={note.id} note={note} onClick={setCurrentNote} />
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 编写 DesktopLayout.tsx**

```typescript
// packages/web/src/components/layouts/DesktopLayout.tsx
import Sidebar from "../desktop/Sidebar";
import NoteView from "../NoteView";
import QuickNote from "../QuickNote";
import { useNotesStore } from "../../stores";

export default function DesktopLayout() {
  const currentNote = useNotesStore((s) => s.currentNote);

  return (
    <div className="flex h-screen">
      <Sidebar />
      <div className="flex-1 h-full overflow-auto">
        {currentNote ? <NoteView note={currentNote} /> : <QuickNote />}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 更新 App.tsx**

```typescript
// packages/web/src/App.tsx
import { useEffect, useState } from "react";
import { initStorage } from "./lib";
import { useResponsive } from "./hooks";
import DesktopLayout from "./components/layouts/DesktopLayout";
import MobileLayout from "./components/layouts/MobileLayout";

export default function App() {
  const [ready, setReady] = useState(false);
  const { isMobile } = useResponsive();

  useEffect(() => {
    initStorage().then(() => setReady(true));
  }, []);

  if (!ready) return <div className="flex items-center justify-center h-screen text-gray-500">正在初始化...</div>;

  return isMobile ? <MobileLayout /> : <DesktopLayout />;
}
```

- [ ] **Step 4: Commit**

```bash
git add packages/web/src/components/layouts/DesktopLayout.tsx packages/web/src/components/desktop/Sidebar.tsx packages/web/src/App.tsx
git commit -m "feat: 实现桌面端双栏布局（侧栏 + 编辑区）"
```

---

### Task 22: 移动端堆栈布局

**Files:**
- Create: `packages/web/src/components/layouts/MobileLayout.tsx`
- Create: `packages/web/src/components/mobile/NoteListMobile.tsx`

- [ ] **Step 1: 编写 NoteListMobile.tsx**

```typescript
// packages/web/src/components/mobile/NoteListMobile.tsx
import { useEffect } from "react";
import { useStorage } from "../../hooks";
import { useNotesStore } from "../../stores";
import NoteCard from "../shared/NoteCard";

export default function NoteListMobile() {
  const { listNotes } = useStorage();
  const { notes, setNotes, setCurrentNote } = useNotesStore();

  useEffect(() => {
    listNotes().then(setNotes);
  }, []);

  const activeNotes = notes.filter((n) => n.deletedAt === null);

  return (
    <div className="flex flex-col h-full">
      <div className="p-3 border-b border-gray-200">
        <h2 className="text-lg font-bold text-gray-800">← 全部笔记</h2>
      </div>
      <div className="flex-1 overflow-auto p-2 space-y-2">
        {activeNotes.map((note) => (
          <NoteCard key={note.id} note={note} onClick={setCurrentNote} />
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 编写 MobileLayout.tsx**

```typescript
// packages/web/src/components/layouts/MobileLayout.tsx
import { useState } from "react";
import { useNotesStore, useUIStore } from "../../stores";
import NoteView from "../NoteView";
import NoteListMobile from "../mobile/NoteListMobile";
import QuickNote from "../QuickNote";

export default function MobileLayout() {
  const currentNote = useNotesStore((s) => s.currentNote);
  const setCurrentNote = useNotesStore((s) => s.setCurrentNote);
  const [showList, setShowList] = useState(false);

  const handleBack = () => {
    setCurrentNote(null);
  };

  return (
    <div className="flex flex-col h-screen">
      {currentNote ? (
        <NoteView note={currentNote} onBack={handleBack} />
      ) : showList ? (
        <NoteListMobile />
      ) : (
        <QuickNote />
      )}

      <div className="flex justify-around py-2 border-t border-gray-200 bg-gray-50">
        <button
          onClick={() => { setCurrentNote(null); setShowList(false); }}
          className="text-xs text-blue-500 font-semibold"
        >
          📝 快速笔记
        </button>
        <button
          onClick={() => { setCurrentNote(null); setShowList(true); }}
          className="text-xs text-gray-600"
        >
          📋 全部笔记
        </button>
        <button className="text-xs text-gray-600">🔍 搜索</button>
        <button className="text-xs text-gray-600">⚙️</button>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add packages/web/src/components/layouts/MobileLayout.tsx packages/web/src/components/mobile/NoteListMobile.tsx
git commit -m "feat: 实现移动端堆栈导航布局（快速笔记/列表/编辑）"
```

---

### Task 23: Vitest Web 端配置 + 编辑器测试

**Files:**
- Modify: `packages/web/vitest.config.ts` (添加测试配置)
- Create: `packages/web/tests/components/QuickNote.test.tsx`
- Create: `packages/web/tests/components/NoteView.test.tsx`

- [ ] **Step 1: 更新 web vitest.config.ts**

```typescript
// packages/web/vitest.config.ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import { resolve } from "path";

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@notes/core": resolve(__dirname, "../core/src/index.ts"),
    },
  },
  test: {
    environment: "happy-dom",
    globals: true,
  },
});
```

在 `packages/web/package.json` 的 devDependencies 中添加 `@testing-library/react` 和 `@testing-library/jest-dom`:
```
"@testing-library/react": "^16",
"@testing-library/jest-dom": "^6"
```

Run: `pnpm install`

- [ ] **Step 2: 编写 QuickNote 组件测试**

```typescript
// packages/web/tests/components/QuickNote.test.tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import QuickNote from "../../src/components/QuickNote";

// Mock StorageAdapter
vi.mock("../../src/lib", () => ({
  initStorage: vi.fn(),
  getStorage: vi.fn(() => ({
    createNote: vi.fn(),
    listNotes: vi.fn(() => Promise.resolve([])),
    createFolder: vi.fn(),
    listFolders: vi.fn(() => Promise.resolve([])),
  })),
}));

describe("QuickNote 组件", () => {
  it("应显示快速笔记标题和输入框", () => {
    render(<QuickNote />);
    expect(screen.getByText("⚡ 快速笔记")).toBeDefined();
    expect(screen.getByPlaceholderText("想写点什么？")).toBeDefined();
  });
});
```

- [ ] **Step 3: 编写 NoteView 组件测试**

```typescript
// packages/web/tests/components/NoteView.test.tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import NoteView from "../../src/components/NoteView";
import { Note } from "@notes/core";

const mockNote: Note = {
  id: "test-note-1",
  title: "测试笔记",
  contentJson: '{"type":"doc","content":[{"type":"paragraph","content":[{"type":"text","text":"测试内容"}]}]}',
  mdText: "测试内容",
  folderId: null,
  type: "rich",
  createdAt: Date.now(),
  updatedAt: Date.now(),
  deletedAt: null,
  version: 1,
};

describe("NoteView 组件", () => {
  it("应显示笔记标题", () => {
    render(<NoteView note={mockNote} />);
    expect(screen.getByText("测试笔记")).toBeDefined();
  });

  it("应显示模式切换按钮", () => {
    render(<NoteView note={mockNote} />);
    expect(screen.getByText("所见即所得")).toBeDefined();
    expect(screen.getByText("Markdown")).toBeDefined();
  });
});
```

- [ ] **Step 4: 运行 Web 端测试**

Run: `cd packages/web && pnpm vitest run`

Expected: 组件测试通过（可能有 TipTap WASM/渲染相关问题需要修复）

- [ ] **Step 5: Commit**

```bash
git add packages/web/vitest.config.ts packages/web/tests/ packages/web/package.json
git commit -m "feat: 配置 Web 端 Vitest + 编写组件测试"
```

---

### Task 24: 编辑器样式优化

**Files:**
- Modify: `packages/web/src/styles/globals.css` (添加 ProseMirror 编辑器样式)

- [ ] **Step 1: 添加编辑器样式到 globals.css**

在现有 globals.css 末尾追加：

```css
/* TipTap / ProseMirror 编辑器样式 */
.ProseMirror {
  outline: none;
}

.ProseMirror p.is-editor-empty:first-child::before {
  color: #adb5bd;
  content: attr(data-placeholder);
  float: left;
  height: 0;
  pointer-events: none;
}

.ProseMirror h1 {
  font-size: 1.5rem;
  font-weight: 700;
  line-height: 1.3;
  margin-bottom: 0.5rem;
}

.ProseMirror h2 {
  font-size: 1.25rem;
  font-weight: 600;
  line-height: 1.4;
  margin-bottom: 0.5rem;
}

.ProseMirror h3 {
  font-size: 1.1rem;
  font-weight: 600;
  line-height: 1.5;
  margin-bottom: 0.5rem;
}

.ProseMirror ul {
  list-style-type: disc;
  padding-left: 1.5rem;
  margin-bottom: 0.5rem;
}

.ProseMirror ol {
  list-style-type: decimal;
  padding-left: 1.5rem;
  margin-bottom: 0.5rem;
}

.ProseMirror pre {
  background-color: #f1f5f9;
  border-radius: 0.5rem;
  padding: 0.75rem 1rem;
  font-family: monospace;
  font-size: 0.875rem;
  overflow-x: auto;
}

.ProseMirror code {
  background-color: #f1f5f9;
  border-radius: 0.25rem;
  padding: 0.125rem 0.25rem;
  font-family: monospace;
  font-size: 0.875rem;
}

.ProseMirror pre code {
  background-color: transparent;
  padding: 0;
}

.ProseMirror blockquote {
  border-left: 3px solid #e2e8f0;
  padding-left: 1rem;
  margin-left: 0;
  color: #64748b;
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/web/src/styles/globals.css
git commit -m "feat: 添加 TipTap/ProseMirror 编辑器样式"
```

---

### Task 25: 验证与集成测试

**Files:**
- None (验证现有代码)

- [ ] **Step 1: 运行所有 core 测试**

Run: `cd packages/core && pnpm vitest run`

Expected: 13 passed, 18 skipped (WASM tests)

- [ ] **Step 2: 运行所有 web 测试**

Run: `cd packages/web && pnpm vitest run`

Expected: 组件测试通过

- [ ] **Step 3: 启动 Web 应用进行浏览器验证**

Run: `cd packages/web && pnpm dev`

在浏览器 http://localhost:3000 中验证：
1. 应用正常启动，显示"正在初始化..."后切换到主界面
2. 桌面端：双栏布局（侧栏 + 快速笔记/编辑区）
3. 移动端（调整窗口宽度 < 640px）：堆栈导航布局
4. 快速笔记输入框可用
5. 模式切换（所见即所得 ↔ Markdown）工作正常
6. 编辑器中输入内容后自动保存

- [ ] **Step 4: 修复浏览器验证中发现的问题**

根据浏览器实际运行情况修复问题。常见问题可能包括：
- wa-sqlite WASM 加载路径配置
- TipTap 编辑器在 React StrictMode 下重复初始化
- Markdown 序列化器转换不准确

- [ ] **Step 5: 最终 Commit**

```bash
git add -A
git commit -m "fix: 修复浏览器验证中发现的问题"
```

---

## 自审

### 1. 规格覆盖检查

| 规格章节 | 覆盖的 Task |
|----------|-------------|
| TipTap StarterKit 集成 | Task 12, 16 |
| Markdown 快捷输入 | Task 12 (StarterKit 含 keepMarks) |
| 模式切换（WYSIWYG ↔ MD） | Task 17, 18, 19 |
| 自动保存 (debounce 500ms) | Task 19 (NoteView 内 setTimeout) |
| 首屏快速笔记 | Task 20 |
| 桌面端双栏布局 | Task 21 |
| 移动端堆栈导航 | Task 22 |
| 标题自动提取 | Task 19 (extractTitleFromContent) |
| 响应式切换 | Task 14, 21, 22 |
| 编辑器样式 | Task 24 |

**缺失项：**
- Markdown 快捷输入的详细规则（`#` → H1, `**` → bold 等）已由 StarterKit + keepMarks 配置覆盖，但可能需要更多自定义
- 斜杠命令（`/` → 命令面板）属于 P3 范围，不在 P2 中

### 2. 占位符扫描

无 TBD/TODO。所有代码均有完整实现。

### 3. 类型一致性

- `Note` 类型与 P1 定义的模型一致
- `SearchResult` 在 useStorage hook 中引用了 P1 的定义
- `useNotesStore.setCurrentNote` 参数类型为 `Note | null`
- `extractTitleFromContent` 接受 `string` 输入，返回 `string`