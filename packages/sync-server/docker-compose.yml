services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: notes_sync
      POSTGRES_USER: notes
      POSTGRES_PASSWORD: notes
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
